export interface DefaultRewardData {
    id: number;
    _id: string;
    rewardName: string;
    rewardValue: number;
    rewardActive: boolean;
    index: number;
    // Add other properties as needed
}